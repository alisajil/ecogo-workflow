---
description: "ecogo-workflow — init, migrate, ingest, compile, query, correct, rationale, eval, gap, fetch, learn, lint, run, or remove a knowledge base."
argument-hint: "init <name> | migrate [--dry-run] | ingest <path|url> | compile [<path>] | query <question> | correct <page-slug> [<claim>] | rationale [<subcmd>] | eval [<question>] | gap [<question>] | fetch [<gap-id|question>] | learn [<subcmd>] | lint | run [<profile>] | remove <name>"
---

Load and follow the `ecogo-workflow:ecogo` skill. Pass through all user arguments exactly as provided.
